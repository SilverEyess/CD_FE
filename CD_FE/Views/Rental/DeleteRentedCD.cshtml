@model CD_FE.Models.RentalItem

@{
    ViewBag.Title = "DeleteRentedCD";
}

<h2>DeleteRentedCD</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>RentalItem</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.RentalId)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.RentalId)
        </dd>

        <dt>
            CD Name
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CDs.Where(m => m.Value.Equals(model.CDId.ToString())).Single().Text)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
