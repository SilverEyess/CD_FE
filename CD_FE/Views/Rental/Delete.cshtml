@model CD_FE.ViewModels.RentalDetailsViewModel

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Rental</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.StaffFirstName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.StaffFirstName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.StaffLastName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.StaffLastName)
        </dd>

    </dl>

    <div>
        <h4>CDs Borrowed</h4>
        <ul>
            @foreach(var cds in Model.RentedCDs)
            {
                <li>@cds.CDTitle</li>
            }
        </ul>
    </div>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
